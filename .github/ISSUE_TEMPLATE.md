# Issue

## What happened?

Describe the bug, question, or proposal.

## Expected behavior

What did you expect openRender to do?

## Reproduction

```bash
openrender --version
openrender scan
openrender compile sprite --from tmp/slime_raw.png --id enemy.slime.idle --frames 6 --frame-size 64x64 --dry-run --json
```

## Environment

- OS:
- Node.js:
- pnpm:
- openRender command:

## Scope check

openRender v0.1 is local-first, image-only, and Vite + Phaser focused. Do not attach private game source files, credentials, generated assets, or local reports unless they are safe to share publicly.
