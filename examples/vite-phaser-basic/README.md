# Vite Phaser Basic

This is the v0.1 POC target project for openRender.

Use it to validate:

- `openrender scan`
- `openrender init`
- Phaser asset path generation
- future compile/install/verify/report/rollback commands

Run:

```bash
pnpm --filter vite-phaser-basic dev
```
