# LOVE2D Minimal

Minimal LOVE2D project for openRender local smoke checks.

```bash
openrender context --json --compact
openrender smoke --target love2d --json
```

Generated assets should be installed under `assets/openrender/` and helper Lua files under `openrender/`.
